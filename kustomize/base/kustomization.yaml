apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: webapp-base

resources:
- namespace.yaml
- configmap.yaml
- deployment.yaml
- service.yaml

commonLabels:
  app.kubernetes.io/name: webapp
  app.kubernetes.io/component: web
  app.kubernetes.io/part-of: webapp-system

images:
- name: nginx
  newTag: 1.21-alpine
